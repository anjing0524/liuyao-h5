# 交付验证

- Rust：27 项测试通过，含装卦、纳甲、干支、概率与新增叶面契约。
- JavaScript：5 项测试通过，直接加载真实 WASM。
- 1,000 次三叶正反面与阴阳、动静、爻类型一致。
- 乾坤全动爻本卦/变卦、装卦字段、种子复现通过。
- Vite 生产构建通过，独立 WASM 约 262 KB。
- 390 × 844 浏览器视口完整六次起卦并展开结果。
- 320 × 568 结果表格无横向溢出。
- 起卦中途返回后可重新进入；结果刷新和卦记重读不重复记卦。
- 独立端口验证生产包加载 WASM 与纹理。

计算结论沿用原仓库算法；本次未重新审定术数算法本身。未替代 iOS Safari / Android 真机验收。

代码基于用户上传归档，发布前已核验其与 GitHub master 的基线版本一致。源码包不含 node_modules 与 Rust target 缓存。

## 统一视觉更新

- 首页、问事、起卦、结果、卦记及小记共用 9:16 shell。
- 390 × 844 视口：首页、问事、起卦和结果实测均为 390 × 693.33，位置一致。
- 320 × 568 视口：shell 为 319.5 × 568，结果装卦表为 277.5px，无横向溢出。
- 1280 × 900 桌面：统一为 480 × 853.33 居中画幅。
- 太极和反向卦环、24 个微粒、两层雾使用 CSS 装饰动画；页面隐藏或进入起卦时暂停；支持减少动态效果。
- 动爻结果「雷火丰 → 泽雷随」及装卦展开已做浏览器视觉检查。
- 本次视觉更新未修改 Rust 计算或起爻数据接口，生产构建通过。
